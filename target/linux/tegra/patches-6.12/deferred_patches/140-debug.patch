--- a/drivers/firmware/tegra/bpmp.c
+++ b/drivers/firmware/tegra/bpmp.c
@@ -696,6 +696,8 @@ static int tegra_bpmp_probe(struct platf
 	if (!bpmp)
 		return -ENOMEM;
 
+	dev_err(&pdev->dev, "TEST 1");
+
 	bpmp->soc = of_device_get_match_data(&pdev->dev);
 	bpmp->dev = &pdev->dev;
 
@@ -736,18 +738,29 @@ static int tegra_bpmp_probe(struct platf
 	if (err < 0)
 		return err;
 
+	dev_err(&pdev->dev, "TEST 2");
+
 	err = tegra_bpmp_request_mrq(bpmp, MRQ_PING,
 				     tegra_bpmp_mrq_handle_ping, bpmp);
+
+	dev_err(&pdev->dev, "TEST 3");
+
 	if (err < 0)
 		goto deinit;
 
 	err = tegra_bpmp_ping(bpmp);
+
+	dev_err(&pdev->dev, "TEST 4");
+
 	if (err < 0) {
 		dev_err(&pdev->dev, "failed to ping BPMP: %d\n", err);
 		goto free_mrq;
 	}
 
 	err = tegra_bpmp_get_firmware_tag(bpmp, tag, sizeof(tag));
+
+	dev_err(&pdev->dev, "TEST 5");
+
 	if (err < 0) {
 		dev_err(&pdev->dev, "failed to get firmware tag: %d\n", err);
 		goto free_mrq;
@@ -757,28 +770,40 @@ static int tegra_bpmp_probe(struct platf
 
 	platform_set_drvdata(pdev, bpmp);
 
+	dev_err(&pdev->dev, "TEST 6");
+
 	err = of_platform_default_populate(pdev->dev.of_node, NULL, &pdev->dev);
+
+	dev_err(&pdev->dev, "TEST 7");
+
 	if (err < 0)
 		goto free_mrq;
 
 	if (of_find_property(pdev->dev.of_node, "#clock-cells", NULL)) {
+		dev_err(&pdev->dev, "TEST 8");
 		err = tegra_bpmp_init_clocks(bpmp);
+		dev_err(&pdev->dev, "TEST 9");
 		if (err < 0)
 			goto free_mrq;
 	}
 
 	if (of_find_property(pdev->dev.of_node, "#reset-cells", NULL)) {
+		dev_err(&pdev->dev, "TEST 10");
 		err = tegra_bpmp_init_resets(bpmp);
+		dev_err(&pdev->dev, "TEST 11");
 		if (err < 0)
 			goto free_mrq;
 	}
 
 	if (of_find_property(pdev->dev.of_node, "#power-domain-cells", NULL)) {
+		dev_err(&pdev->dev, "TEST 12");
 		err = tegra_bpmp_init_powergates(bpmp);
+		dev_err(&pdev->dev, "TEST 13");
 		if (err < 0)
 			goto free_mrq;
 	}
 
+	dev_err(&pdev->dev, "TEST 14");
 	err = tegra_bpmp_init_debugfs(bpmp);
 	if (err < 0)
 		dev_err(&pdev->dev, "debugfs initialization failed: %d\n", err);
--- a/drivers/firmware/tegra/bpmp.c
+++ b/drivers/firmware/tegra/bpmp.c
@@ -701,36 +701,47 @@ static int tegra_bpmp_probe(struct platf
 	bpmp->soc = of_device_get_match_data(&pdev->dev);
 	bpmp->dev = &pdev->dev;
 
+	dev_err(&pdev->dev, "TEST 30");
+
 	INIT_LIST_HEAD(&bpmp->mrqs);
+	dev_err(&pdev->dev, "TEST 31");
 	spin_lock_init(&bpmp->lock);
+	dev_err(&pdev->dev, "TEST 32");
 
 	bpmp->threaded.count = bpmp->soc->channels.thread.count;
 	sema_init(&bpmp->threaded.lock, bpmp->threaded.count);
+	dev_err(&pdev->dev, "TEST 33");
 
 	size = BITS_TO_LONGS(bpmp->threaded.count) * sizeof(long);
 
 	bpmp->threaded.allocated = devm_kzalloc(&pdev->dev, size, GFP_KERNEL);
+	dev_err(&pdev->dev, "TEST 34");
 	if (!bpmp->threaded.allocated)
 		return -ENOMEM;
 
 	bpmp->threaded.busy = devm_kzalloc(&pdev->dev, size, GFP_KERNEL);
+	dev_err(&pdev->dev, "TEST 35");
 	if (!bpmp->threaded.busy)
 		return -ENOMEM;
 
 	spin_lock_init(&bpmp->atomic_tx_lock);
+	dev_err(&pdev->dev, "TEST 36");
 	bpmp->tx_channel = devm_kzalloc(&pdev->dev, sizeof(*bpmp->tx_channel),
 					GFP_KERNEL);
+	dev_err(&pdev->dev, "TEST 37");
 	if (!bpmp->tx_channel)
 		return -ENOMEM;
 
 	bpmp->rx_channel = devm_kzalloc(&pdev->dev, sizeof(*bpmp->rx_channel),
 	                                GFP_KERNEL);
+	dev_err(&pdev->dev, "TEST 38");
 	if (!bpmp->rx_channel)
 		return -ENOMEM;
 
 	bpmp->threaded_channels = devm_kcalloc(&pdev->dev, bpmp->threaded.count,
 					       sizeof(*bpmp->threaded_channels),
 					       GFP_KERNEL);
+	dev_err(&pdev->dev, "TEST 39");
 	if (!bpmp->threaded_channels)
 		return -ENOMEM;
 
--- a/drivers/firmware/tegra/bpmp-tegra210.c
+++ b/drivers/firmware/tegra/bpmp-tegra210.c
@@ -160,35 +160,49 @@ static int tegra210_bpmp_channel_init(st
 
 static int tegra210_bpmp_init(struct tegra_bpmp *bpmp)
 {
+	//dev_err(&pdev->dev, "TEST 40");
 	struct platform_device *pdev = to_platform_device(bpmp->dev);
 	struct tegra210_bpmp *priv;
 	struct resource *res;
 	unsigned int i;
 	int err;
 
+	dev_err(&pdev->dev, "TEST 41");
+
 	priv = devm_kzalloc(&pdev->dev, sizeof(*priv), GFP_KERNEL);
 	if (!priv)
 		return -ENOMEM;
 
 	bpmp->priv = priv;
 
+	dev_err(&pdev->dev, "TEST 42");
+
 	res = platform_get_resource(pdev, IORESOURCE_MEM, 0);
+	dev_err(&pdev->dev, "TEST 43");
 	priv->atomics = devm_ioremap_resource(&pdev->dev, res);
+	dev_err(&pdev->dev, "TEST 44");
 	if (IS_ERR(priv->atomics))
 		return PTR_ERR(priv->atomics);
 
 	res = platform_get_resource(pdev, IORESOURCE_MEM, 1);
+	dev_err(&pdev->dev, "TEST 45");
 	priv->arb_sema = devm_ioremap_resource(&pdev->dev, res);
+	dev_err(&pdev->dev, "TEST 46");
 	if (IS_ERR(priv->arb_sema))
 		return PTR_ERR(priv->arb_sema);
 
+	dev_err(&pdev->dev, "TEST 47");
+
 	err = tegra210_bpmp_channel_init(bpmp->tx_channel, bpmp,
 					 bpmp->soc->channels.cpu_tx.offset);
+
+	dev_err(&pdev->dev, "TEST 48");
 	if (err < 0)
 		return err;
 
 	err = tegra210_bpmp_channel_init(bpmp->rx_channel, bpmp,
 					 bpmp->soc->channels.cpu_rx.offset);
+	dev_err(&pdev->dev, "TEST 49");
 	if (err < 0)
 		return err;
 
@@ -201,19 +215,25 @@ static int tegra210_bpmp_init(struct teg
 			return err;
 	}
 
+	dev_err(&pdev->dev, "TEST 50");
+
 	err = platform_get_irq_byname(pdev, "tx");
+
+	dev_err(&pdev->dev, "TEST 51");
 	if (err < 0) {
 		dev_err(&pdev->dev, "failed to get TX IRQ: %d\n", err);
 		return err;
 	}
 
 	priv->tx_irq_data = irq_get_irq_data(err);
+	dev_err(&pdev->dev, "TEST 52");
 	if (!priv->tx_irq_data) {
 		dev_err(&pdev->dev, "failed to get IRQ data for TX IRQ\n");
 		return -ENOENT;
 	}
 
 	err = platform_get_irq_byname(pdev, "rx");
+	dev_err(&pdev->dev, "TEST 53");
 	if (err < 0) {
 		dev_err(&pdev->dev, "failed to get rx IRQ: %d\n", err);
 		return err;
@@ -221,6 +241,7 @@ static int tegra210_bpmp_init(struct teg
 
 	err = devm_request_irq(&pdev->dev, err, rx_irq,
 			       IRQF_NO_SUSPEND, dev_name(&pdev->dev), bpmp);
+	dev_err(&pdev->dev, "TEST 54");
 	if (err < 0) {
 		dev_err(&pdev->dev, "failed to request IRQ: %d\n", err);
 		return err;
--- a/drivers/firmware/tegra/bpmp-tegra210.c
+++ b/drivers/firmware/tegra/bpmp-tegra210.c
@@ -140,12 +140,17 @@ static int tegra210_bpmp_channel_init(st
 	u32 address;
 	void *p;
 
+	dev_err(&pdev->dev, "TEST 60");
+
 	/* Retrieve channel base address from BPMP */
 	writel(index << TRIGGER_ID_SHIFT | TRIGGER_CMD_GET,
 	       priv->atomics + TRIGGER_OFFSET);
+	dev_err(&pdev->dev, "TEST 61");
 	address = readl(priv->atomics + RESULT_OFFSET(index));
+	dev_err(&pdev->dev, "TEST 62");
 
 	p = devm_ioremap(bpmp->dev, address, 0x80);
+	dev_err(&pdev->dev, "TEST 63");
 	if (!p)
 		return -ENOMEM;
 
@@ -153,6 +158,7 @@ static int tegra210_bpmp_channel_init(st
 	channel->ob = p;
 	channel->index = index;
 	init_completion(&channel->completion);
+	dev_err(&pdev->dev, "TEST 64");
 	channel->bpmp = bpmp;
 
 	return 0;
--- a/drivers/firmware/tegra/bpmp-tegra210.c
+++ b/drivers/firmware/tegra/bpmp-tegra210.c
@@ -137,6 +137,7 @@ static int tegra210_bpmp_channel_init(st
 				      unsigned int index)
 {
 	struct tegra210_bpmp *priv = bpmp->priv;
+	struct platform_device *pdev = to_platform_device(bpmp->dev);
 	u32 address;
 	void *p;
 
